function countInRange(data, n) {
    let count = 0;
    for (let i = 0; i < data.length; i++) {
        if (data[i] >= data[0] && data[i] <= n) {
            count++;
        }
    }
    return count;
}